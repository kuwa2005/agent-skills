# Optional skills — NOT installed by --all / bare curl|bash
# Install with --everything, or explicitly, e.g.:
#   curl ... | bash -s -- --everything
#   curl ... | bash -s -- frontend-design
frontend-design
split-to-prs
babysit
create-skill
playwright-coreserver
